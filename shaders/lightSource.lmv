#version 330 core

layout (location = 0) in vec3 localPos;

layout (std140) uniform matrices {
	mat4 view;
	mat4 projection;
};

uniform mat4 lightModel;

void main() {
	gl_Position = projection * view * lightModel * vec4(localPos, 1.0f);
}